apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - route.yaml

configMapGenerator:
  - name: web-message
    literals:
      - APP_MESSAGE=Hello from Kustomize base
